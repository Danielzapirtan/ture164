#include <stdio.h>
#include <stdlib.h>

#define NDEC (31)

int hpd[NDEC];

void init(void) {
	for (int i = 0; i < NDEC; i++)
		hpd[i] = -1;
}

int main(void) {
	int holidays = 11;
	int holishifts = 4;
	int workshifts = 9;
	int blankdays = 11;
}

